title: "shivam's blog"
description: >-
  not that serious
baseurl: "/blog" # the subpath of your site
url: "https://shivamhwp.github.io" # replace with your GitHub username
permalink: /:year/:month/:day/:title/
theme: minima

# Build settings
markdown: kramdown
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# Exclude from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor
  - .sass-cache/
